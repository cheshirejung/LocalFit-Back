spring:
  datasource:
    url: ${LOCAL_DB_URL:jdbc:mariadb://localhost:3306/1Team}
    username: ${LOCAL_DB_USERNAME:root}
    password: ${LOCAL_DB_PASSWORD:}
  quartz:
    job-store-type: jdbc
    jdbc:
      initialize-schema: never
